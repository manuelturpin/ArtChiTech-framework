# Bonsai Tracker - Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=development  # development | production

# Database Configuration
USE_SQLITE=true       # true for SQLite, false/unset for in-memory
DB_PATH=./data/bonsai.db  # Path to SQLite database file
DB_MEMORY=false       # true to use in-memory SQLite (testing)

# Security - CORS (comma-separated origins)
# Production: set to your actual domain(s)
ALLOWED_ORIGINS=https://bonsai-tracker.com,https://app.bonsai-tracker.com

# Sentry - Error Monitoring (optional)
# Get your DSN from https://sentry.io
SENTRY_DSN=

# Rate Limiting (configured in code)
# Development: 1000 requests/15min
# Production: 100 requests/15min
