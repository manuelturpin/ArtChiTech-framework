{
  "name": "bonsai-tracker",
  "version": "0.1.0",
  "description": "SaaS B2B pour pépiniéristes - traçabilité bonsaïs multi-sites",
  "type": "module",
  "scripts": {
    "dev": "tsc && node dist/api/server.js",
    "cli": "tsc && node dist/cli/index.js",
    "start": "npm run dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "tsc",
    "lint": "eslint src/"
  },
  "bin": {
    "bonsai-tracker": "./dist/cli/index.js"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "helmet": "^8.1.0"
  }
}
