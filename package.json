{
  "name": "bonsai-tracker",
  "version": "0.1.0",
  "description": "SaaS B2B pour pépiniéristes - traçabilité bonsaïs multi-sites",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "tsc",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
