# ACT v2.5 - Optional ignores
# Uncomment lines below based on your needs

# Research findings (if confidential)
# .act/findings.md

# Local hooks (if project-specific)
# .act/hooks.json

# Progress logs (if too verbose for VCS)
# .act/progress.md

# Sensitive session data
.act/history/
.act/handoffs/
.act/observations.jsonl
.act/logs/
